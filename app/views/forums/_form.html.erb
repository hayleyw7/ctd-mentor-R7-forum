<%= form_with(model: forum) do |form| %>
  <% if forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(forum.errors.count, "error") %> prohibited this forum from being saved:</h2>

      <ul>
        <% forum.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :forum_name, "Name" %>
    <%= form.text_field :forum_name, required: true %>
  </div>

  <div class="field">
    <%= form.label :description, "Description" %>
    <%= form.text_area :description, required: true %>
  </div>

  <div class="actions">
    <%= form.submit 'Create Forum' %>
  </div>
<% end %>
